version: "3.8"

services:
  app:
    container_name: app-dev
    # image: app-dev
    build: 
      context: .
      target: development
      dockerfile: Dockerfile
    volumes: 
      - ./src:/app/src
    env_file:
      - ./.env
    ports:
      - 3000:3000